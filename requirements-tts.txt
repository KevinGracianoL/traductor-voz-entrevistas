chatterbox-tts==0.1.7
# Instalación aislada (incompatible con torch==2.13.0+cu132 / numpy==2.4.4 del repo):
#   python -m venv venv-tts && venv-tts/Scripts/activate
#   pip install -r requirements-tts.txt && pip check
# 0.1.7 exige torch==2.6.0, torchaudio==2.6.0 y numpy<2 en Python 3.12.
# Hipótesis sin medir: ~2-3 GB VRAM y 400-700 ms primer chunk en GTX 1650 Ti.
# Uso: from traductor.tts.chatterbox import sintetizar; sintetizar("hola", "ref.wav")
