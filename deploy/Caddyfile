traductor-demo.kevingraciano.dev {
    reverse_proxy localhost:8000

    # Demo: solo feed estático, sin audio real
    # TLS auto via Caddy (Let's Encrypt), cabe en 947 MB
    encode gzip
    header {
        X-Frame-Options "SAMEORIGIN"
        X-Content-Type-Options "nosniff"
    }
}
